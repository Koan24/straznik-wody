# Frontend Configuration
# Note: API URL is currently hardcoded in src/services/*.js
# If you want to use environment variables, uncomment the line below
# and update all service files to use: import.meta.env.VITE_API_URL || "http://localhost:4000"

VITE_API_URL=http://localhost:4000
